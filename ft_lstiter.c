#include "libft.h"

void	ft_lstiter(t_list *lst, void (*f)(void *))
{

}

int main()
{
	t_list *head;
	head = malloc(sizeof(t_list));
	head->content = ft_strdup("33");
	head->next = ft_strdup("44");

	t_list	*current;
	current = malloc
}